export function getVisibleColumns(visibilityResolver) {
    return visibilityResolver.findVisibleColumns();
}

export function getVisibleRows(visibilityResolver) {
    return visibilityResolver.findVisibleRows();
}
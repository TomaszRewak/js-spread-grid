import WorkInProgress from "../../components/WorkInProgress";

export default function Headers() {
    // Write about header text, resizing and reordering (how it can be disabled) here.
    return (
        <>
            <WorkInProgress step="documentation" />
        </>
    );
}
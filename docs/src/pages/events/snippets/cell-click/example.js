SpreadGrid(div, {
    data: {
        // collapse: true
        // default dict data
        // collapse: false
    },
    onCellClick: ({ columnId, rowId, ctrlKey, shiftKey, button, buttons, detail }) => {
        // ...
    }
});